CREATE TABLE st_picture (id INTEGER PRIMARY KEY AUTOINCREMENT, record_id INTEGER, record_model VARCHAR(50), position INTEGER, caption VARCHAR(500), image VARCHAR(500), image_width INTEGER, image_height INTEGER, thumbnail VARCHAR(500), thumbnail_width INTEGER, thumbnail_height INTEGER, source VARCHAR(500), is_published INTEGER DEFAULT '0', params TEXT, created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL);
